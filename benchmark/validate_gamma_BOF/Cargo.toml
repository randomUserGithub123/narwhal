[package]
name = "validate_gamma_batch_fairness"

[dependencies]
clap = { version = "4.4", features = ["derive"] }
rayon = "1.8"
regex = "1.10"
glob = "0.3"
petgraph = "0.8"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1

[profile.dev]
opt-level = 1
