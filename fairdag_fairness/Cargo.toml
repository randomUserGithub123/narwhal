[package]
name = "fairdag_fairness"
version = "0.1.0"
edition = "2021"
description = "FairDAG-RL fairness layer: dependency graph construction and ordering finalization"

[dependencies]
crypto = { path = "../crypto" }
worker = { path = "../worker" }
log = "0.4"
serde = { version = "1.0", features = ["derive"] }

[dev-dependencies]
# Enable test helpers (generate_keypair, etc.) from the crypto crate
crypto = { path = "../crypto" }
env_logger = "0.9"
